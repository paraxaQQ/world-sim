[project]
name = "world-sim"
version = "0.7.0"
description = "A deterministic, closed-world social-survival experiment for named model agents."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
world-sim = "world_sim.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
