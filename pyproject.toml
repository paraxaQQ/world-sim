[project]
name = "world-sim"
version = "0.2.0"
description = "A deterministic, locally contained selection and verification experiment."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
world-sim = "world_sim.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
