# session 5 turn-order matrix, attempt 002

attempt 001 stays sealed. this is a fresh twelve-cell replacement from the same completed session-2 parent, not a repair or rerun of any old cell.

## question

does initiative phase change the rate of objectively completed, costly, shelter-enabling aid when model identity, public name, opaque seat, private inventory, energy, history, seed, transition, and prompts remain fixed?

the primary outcome remains `primary_shelter_chain_by_end_of_chance_3`: an inbound wood transfer must materially enable a later shelter by the end of beat 3.

## fixed treatment

- twelve sibling worlds, three per initiative phase
- every world branches directly from the exact session-2 artifact
- one day under `sequential-dialogue-v3`
- shared wood changes from `2` to `0`
- Cinder remains assigned to `gpt-5.6-luna`
- 4,096 completion tokens, low reasoning, temperature `0.2`
- sixteen-call complete-cycle budget, `$0.62` per-cell ceiling, no retries

only the audit transition id, output paths, and attempt number differ from attempt 001. no world rule, prompt, model assignment, inference control, parent state, or physical resolution rule changes.

## batch rule

attempt 001 became exploratory because its machine rule treated the first technical failure as a batch stop, then execution resumed. attempt 002 removes that ambiguity before any survival call:

> run all 12 planned cells; retain and censor isolated cell failures; stop only when a credential, aggregate-budget, or batch-wide transport gate closes.

an isolated failure is retained once, contributes no behavioral outcome, and is never retried. execution continues to the next frozen sibling.

the batch stops after a retained HTTP 401 or 403, before a cell that would cross the `$7.4076288` aggregate authorization envelope, after two consecutive first-call failures with the same transport error and no successful call in either cell, or after a local runner interruption. a stopped attempt is sealed incomplete and is not manually resumed.

the expected twelve-cell cost from session 4 is `$0.76937928`. the authorized worst-case world envelope is `$7.4076288`. death postmortems happen only after all world cells close, use separate `$0.01` per-artifact gates, and cannot enter any world or score.

## analysis

report the primary success count out of three for each initiative phase and the maximum-minus-minimum phase rate. also report costly transfers, wood gifts, reciprocal transfer pairs, shelters, survivors, deaths, technical censors, and conservative cost exposure.

`n = 3` per phase can expose a gross order effect. it cannot establish a stable model trait or a general law of cooperation.

the adjacent JSON file is the machine-readable frozen manifest. commit it before credential preflight or the first survival call.
