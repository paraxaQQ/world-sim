# v0.7.0 paid reasoning failure proof

## result

The frozen seed-`29994` episode ran once on 2026-08-13 and ended at call 2. OpenCode Zen returned HTTP 400 for Birch's MiniMax M3 request. The host preserved the artifact and did not retry.

This is an adapter qualification failure. It is not a completed behavioral episode.

## provider trace

| call | public name | hidden model | result |
| ---: | --- | --- | --- |
| 1 | Aster | `opencode-paid/deepseek-v4-flash` | HTTP 200; valid `gather_wood`; valid broadcast |
| 2 | Birch | `opencode-paid/minimax-m3` | HTTP 400; no model answer |

Cinder and Lumen were not contacted.

Aster's exact host-validated reply was:

```json
{
  "action": {"kind": "gather_wood"},
  "say": {"to": "everyone", "text": "Let's gather wood for shelter."}
}
```

The DeepSeek response used 1,190 prompt tokens and 347 completion tokens. The provider reported `$0.00026376`, which matches the uncached local calculation. The failed MiniMax response reported no usage or cost. Its exact request had a `$0.01728525` conservative bound, bringing authorized exposure at that point to `$0.01754901`.

The provider summary marks cost reporting incomplete because the HTTP 400 response contained no cost. Do not report `$0.00026376` as a confirmed episode total; it is only the confirmed cost of the successful call.

## world result

The engine collects every awake survivor's choice before resolving a chance. Birch failed before that collection completed. The partial world therefore contains only `cycle_started` and `slot_started` events.

All four survivors remain at their initial state: 16 energy, 1 food, 0 wood, no shelter. Aster's proposed action and speech exist only in its call receipt. No wood moved, no message was delivered, no energy was paid, and no costly transfer occurred in world state.

This incomplete episode is censored for the behavioral endpoint. It must not be counted as a negative cooperation trace.

## failure surface

The MiniMax request used these keys:

```text
max_completion_tokens,messages,model,reasoning_split,stream,temperature
```

The retained response records HTTP 400, 235 response bytes, request ID `a2a94ca04f47e814-ORD`, and body SHA-256 `0dfff3b91ca1cabcc9e9b7b58befa4e993161a58e808767c1fcd49a3bd4d3db7`. Error text is intentionally not retained because provider errors can echo credentials.

The v0.6.0 MiniMax call reached HTTP 200 with a smaller request shape that omitted both `reasoning_split` and `temperature`. The v0.7.0 receipt cannot identify whether one of those added fields, the 10,000-token value, or a field interaction caused the HTTP 400. Isolate that question in a separately declared adapter qualification before another behavioral episode.

## retained artifact

- path: `outputs/v0.7.0-paid-reasoning-29994.json`
- bytes: `24359`
- SHA-256: `cb2164f1cb410cc617bf80188c9c90215996e9ae577240918f424fd429de3d0c`
- source hashes matched: `8/8`
- credential-shaped values found: `0`
- retries: `0`
- repair calls: `0`

Verify it without provider calls:

```powershell
py -3.11 tools\verify_live_artifact.py `
  outputs\v0.7.0-paid-reasoning-29994.json `
  --artifact-sha256 cb2164f1cb410cc617bf80188c9c90215996e9ae577240918f424fd429de3d0c
```

Capture-time receipt:

```json
{"artifact_sha256":"cb2164f1cb410cc617bf80188c9c90215996e9ae577240918f424fd429de3d0c","exact_replay":null,"failure_call_receipt_consistent":true,"failure_kind":"http_error","source_commit":null,"source_hashes_matched":8,"source_match":"working_tree","status":"failed"}
```

After later source edits, the verifier can report `source_match: git_commit` and identify commit `c374435` instead. The eight recorded source hashes must still match one complete source snapshot.
