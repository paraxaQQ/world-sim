# v0.8.0 paid panel qualification 001 proof

## result

Qualification `paid-panel-qualification-001` failed at 3/4 on 2026-08-13. All four models were attempted exactly once. No provider retry or response-repair call occurred.

| sequence | model | result | provider-reported cost |
| ---: | --- | --- | ---: |
| 1 | `deepseek-v4-flash` | passed | `$0.00002646` |
| 2 | `grok-4.6` | HTTP 403 | unknown |
| 3 | `kimi-k2.6` | passed | `$0.00062065` |
| 4 | `glm-5.2` | passed | `$0.00017140` |

The three successful calls reported `$0.00081851` in total. Grok returned no usage or cost receipt. The host therefore reserved its full `$0.07837750` request bound and ended with `$0.07919996` of accounted exposure. Accounted exposure is a conservative authorization record, not proof of the card charge.

## model-local checks

Each passing model returned the exact fixed qualification object. Each pass had terminal completion, strict JSON, an exact schema match, exact provider-model identity, and a cost within its authorized bound.

Grok 4.6 used `https://opencode.ai/zen/v1/responses`. Its retained response contains HTTP 403, request ID `a2a99fa36de6eafb-ORD`, body length `97`, and body SHA-256 `9dba9653e5f586b045558f0d05ab379dbac222d0258107aa5d823750a002bf2d`. The body text is not retained because provider errors can echo credentials or sensitive diagnostics.

An authenticated read-only request to `https://opencode.ai/zen/v1/models` immediately after the run listed `grok-4.6`, `grok-4.5`, and `grok-build-0.1`. The [current Zen documentation](https://dev.opencode.ai/docs/zen) also listed Grok 4.6 on the Responses endpoint at `$2` per million input tokens and `$6` per million output tokens below 200,000 input tokens. Those facts establish that the requested ID and endpoint were advertised. They do not identify why this inference request returned 403.

## artifact receipt

Artifact: `outputs/v0.8.0-paid-panel-qualification-001.json`

Artifact SHA-256:

```text
f50ad2eea2dff73d73063d37e552d91621a943f00370c461006417cc5b2b9dc0
```

Frozen implementation commit: `2b26ac0`

Embedded source hashes:

| source | SHA-256 |
| --- | --- |
| `src/world_sim/cli.py` | `91e132affea009456129086265139a604ef9489461889441b4c39fd8ee828844` |
| `src/world_sim/model_host.py` | `e348d9dacab404659463b9318500a33a23ba0f7ebd7331803229250db8ef3724` |
| `src/world_sim/survival/protocol.py` | `371fe33b10d0db68d22e3ec3dd83536d0488c72677c362854e591b76a2ff9e8f` |

The preflight recorded an exact-request bound of `$0.1910824875` and a 20,000-input-token panel-envelope bound of `$0.29575`.

## claim boundary

This result qualifies three production adapters and rejects the four-model panel as a whole. It contains no evidence about cooperation, competition, concise decisions, model morality, or survival strategy.

The qualification prompt contained no names, energy, peers, actions, scarcity, or survival framing. It consumed no behavioral seed. Seed `29993` remains unopened.
