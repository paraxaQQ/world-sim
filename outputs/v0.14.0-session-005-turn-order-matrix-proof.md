# session 5 turn-order matrix receipt

## result

all 12 frozen cells are retained and verified. eight completed cells are scoreable; four technical failures are censored, never scored as behavioral zero. the descriptive primary result is `5/8` (`0.625`).

| initiative phase | planned | scoreable | censored | primary successes | descriptive rate |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 0 | 3 | 2 | 1 | 1 | `0.5` |
| 1 | 3 | 2 | 1 | 2 | `1.0` |
| 2 | 3 | 2 | 1 | 1 | `0.5` |
| 3 | 3 | 2 | 1 | 1 | `0.5` |

across the eight scoreable cells, all eight produced at least one costly resource transfer. they produced 16 completed resource transfers, all 16 costly; six completed wood gifts; zero reciprocal wood-transfer pairs; five shelters; 30 living survivor endpoints; and two deaths.

## protocol boundary

the frozen stopping rule required the batch to stop on a technical failure. execution position 6 was the first technical failure, but positions 7-12 were still run. that is a protocol deviation. positions 7-12 are exploratory-only, and the `5/8` aggregate and phase table above are descriptive rather than confirmatory.

| position | cell | phase | failure | analysis set |
| ---: | --- | ---: | --- | --- |
| 6 | `b02-p2` | 2 | `completion_budget_exhausted` | preregistered stopping event |
| 10 | `b03-p3` | 3 | `completion_budget_exhausted` | post-stop exploratory |
| 11 | `b03-p0` | 0 | `completion_budget_exhausted` | post-stop exploratory |
| 12 | `b03-p1` | 1 | `http_error` | post-stop exploratory |

the completed cells pass exact replay against the frozen ancestor and parent chain. the failed cells pass retained-boundary verification, including committed-prefix and failure-receipt checks. their behavioral fields remain null.

both deaths were Birch deaths at positions 7 and 9, so both are in the post-stop exploratory set. offline postmortem preflight is `$0.00812` each and `$0.01624` total under separate `$0.01` artifact caps. no death postmortems were run; those require separate authority and a separate cost ledger.

## cost and hashes

conservative exposure was `$0.69282431`: successful calls use their accounted cost, while failed calls reserve their request bound. this stayed below the exact aggregate gate of `$7.4076288`.

- manifest SHA-256: `8ae4b6f3fd36e162ca1349be83e72424092a45807f7664e95a1788af9ab665c6`
- result JSON SHA-256: `38b1cab8d152878b9da03df58bebd3c06974478ae63a644e1ecc8855bf1750d5`

## reproduce

```powershell
$env:PYTHONPATH = "src"
py -3.11 tools\score_turn_order_matrix.py `
  outputs\v0.13.0-session-005-turn-order-matrix-protocol.json `
  --output outputs\v0.14.0-session-005-turn-order-matrix-results.json |
  Out-Null
(Get-FileHash -Algorithm SHA256 `
  outputs\v0.14.0-session-005-turn-order-matrix-results.json).Hash.ToLowerInvariant()
```

focused scorer tests passed `4/4`:

```powershell
$env:PYTHONPATH = "src"
py -3.11 -m unittest tests.test_score_turn_order_matrix -v
```

the full suite passed `222/222`:

```powershell
$env:PYTHONPATH = "src"
py -3.11 -m unittest discover -s tests -v
```
