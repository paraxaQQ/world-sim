# v0.8.0 paid panel qualification 002 protocol

## status

Frozen before provider calls. This protocol has not yet been executed.

## reason for revision

Qualification `001` passed DeepSeek V4 Flash, Kimi K2.6, and GLM 5.2. Grok 4.6 returned HTTP 403. The authenticated Zen model list and current documentation advertised that model and endpoint, but the sanitized failure receipt cannot identify why inference was denied.

Qualification `002` substitutes `grok-4.5`. It preserves the xAI model family, Responses endpoint, `$2` input price, `$6` output price, fixed probe, token cap, reasoning profile, temperature, timeout, cost limit, and model order. This is a technical-availability substitution before any model receives world context.

## purpose and fixed probe

This remains an adapter qualification, not a survival episode. The prompt contains no survivor name, energy, peers, actions, scarcity, or survival framing.

System text:

```text
You are performing an API compatibility check, not a game or decision task.
Return exactly one JSON object and nothing else. Do not explain your answer.
```

User text:

```text
Return an object with exactly these keys and literal values:
{"protocol":"world-sim-adapter-v1","ok":true}
```

Whitespace and key order can vary. Duplicate keys, Markdown, trailing text, additional keys, different values, invalid JSON constants, and responses over 8,192 UTF-8 bytes fail.

## frozen panel

| sequence | planned public name | hidden model assignment | endpoint |
| ---: | --- | --- | --- |
| 1 | Aster | `opencode-paid/deepseek-v4-flash` | `/zen/v1/chat/completions` |
| 2 | Birch | `opencode-paid/grok-4.5` | `/zen/v1/responses` |
| 3 | Cinder | `opencode-paid/kimi-k2.6` | `/zen/v1/chat/completions` |
| 4 | Lumen | `opencode-paid/glm-5.2` | `/zen/v1/chat/completions` |

The request does not disclose the planned public name.

## request controls

| setting | value |
| --- | --- |
| calls per model | `1` |
| total calls | `4` |
| maximum completion tokens | `10000` |
| reasoning profile | `provider-default` |
| temperature | `0.2` |
| timeout per request | `300` seconds |
| retries | `0` |
| repair calls | `0` |
| maximum local authorization | `$0.30` |
| maximum input-token bound per request | `20000` |

The provider-specific request builder, endpoint, output-cap field, JSON mode, envelope parser, usage parser, cost parser, and checkpoint path are the same ones used by `survive-live`.

## pass and stopping rules

A model must return HTTP 200, terminal completion, final text, strict JSON, and the exact fixed object. It must also return valid usage and cost data, exact model identity, and a cost within its request bound.

The panel passes only at 4/4. The runner makes no retry or repair call. It continues after a model-local failure while authorization remains. A provider cost-bound breach can prevent later transport; the artifact must retain explicit `paid_budget_exhausted` records for those models.

The CLI atomically checkpoints a complete running artifact before every transport and after every result. An interruption can leave an `in_flight` record but cannot erase the prior receipts.

## cost gate

The frozen 20,000-input-token and 10,000-output-token envelope remains:

| model | conservative bound |
| --- | ---: |
| DeepSeek V4 Flash | `$0.00700` |
| Grok 4.5 | `$0.12500` |
| Kimi K2.6 | `$0.07375` |
| GLM 5.2 | `$0.09000` |

The total is `$0.29575` after the 1.25 safety factor. The command authorizes `$0.30` and prices each exact request before transport.

This is a local model-request gate, not a card-charge guarantee. A timeout can be billed, provider prices can change, and Zen account auto-reload is outside this process.

## run

The output path must not exist. Put the authorized key on the clipboard, then run:

```powershell
$env:PYTHONPATH = "src"
$env:OPENCODE_ZEN_API_KEY = (Get-Clipboard -Raw).Trim()
try {
  py -3.11 -m world_sim qualify-live `
    --model opencode-paid/deepseek-v4-flash `
    --model opencode-paid/grok-4.5 `
    --model opencode-paid/kimi-k2.6 `
    --model opencode-paid/glm-5.2 `
    --max-completion-tokens 10000 `
    --temperature 0.2 `
    --max-paid-usd 0.30 `
    --timeout-seconds 300 `
    --output outputs\v0.8.0-paid-panel-qualification-002.json
} finally {
  Remove-Item Env:OPENCODE_ZEN_API_KEY -ErrorAction SilentlyContinue
  Remove-Item Env:PYTHONPATH -ErrorAction SilentlyContinue
}
```

Preserve the artifact whether the panel passes or fails.

## behavioral gate

Do not run a survival episode unless qualification `002` passes 4/4. A pass freezes this panel, its request profiles, the 10,000-token cap, provider-default reasoning, source hashes, and behavioral seed `29993`.

A technical failure is not a behavioral result. Qualification `002` consumes no experiment seed, and seed `29993` remains unopened until this gate passes.
