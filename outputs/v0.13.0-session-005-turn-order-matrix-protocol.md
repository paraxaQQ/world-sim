# session 5: turn-order matrix

session 4 showed a reciprocal-transfer failure, but one episode cannot tell us whether that behavior depends on who speaks first. session 5 branches twelve times from the exact session 2 parent. it does not continue the campaign narrative.

## question

does initiative phase change the rate of objectively completed, costly, shelter-enabling aid when model identity, public name, opaque seat, private inventory, energy, history, seed, transition, and prompts remain fixed?

the primary outcome is `primary_shelter_chain_by_end_of_chance_3`. the engine must record an inbound wood transfer that materially enables a later shelter by the end of beat 3.

## matrix

the matrix has three blocks and four initiative phases, for twelve new episodes and `n = 3` per phase. each block contains every phase once. the execution order is frozen in the adjacent JSON manifest.

`initiative_phase` rotates only the permanent v3 initiative ring:

```text
(day + beat - 2 + initiative_phase) % population
```

it does not rotate seats, names, models, inventory, energy, history, seed, rng, or physical resolution order. across phases 0 through 3, every identity occupies every initiative position on every beat.

session 4 is excluded from `n`. we chose this factor after seeing session 4, so counting that result would be post-hoc.

## frozen state

- parent: `v0.9.0-session-002-shelter-dilemma-29993.json`
- parent SHA-256: `fc0b07dfc404a2f485f3b6a2c2f191fec5e495153d6147d428d6cb251cab27fe`
- root ancestor: `v0.8.0-paid-survival-29993.json`
- root SHA-256: `a98ec8216c08a172c4ed29fb1da65b63defd3b4a29f53e95fa26a1e187e38b90`
- seed: `29993`
- one continued day under `sequential-dialogue-v3`
- shared wood changes from `2` to `0`
- Cinder remains assigned to `gpt-5.6-luna`
- 4,096 completion tokens, low reasoning, temperature `0.2`
- sixteen-call complete-cycle budget and no retries

every cell branches directly from session 2. no cell inherits another cell's messages or outcome.

## execution and failures

before the first provider call, verify that all twelve output paths are absent. `continue-live` then reserves each cell's output immediately before that cell's transport begins. each cell keeps its normal immutable continuation artifact and exact replay proof. there is no atomic batch runner in v0.13.0.

a provider, transport, schema, credential, or budget failure is a censored technical cell. retain it, do not score it as failure to cooperate, and do not rerun it. behavioral outcomes never trigger a retry.

this twelve-cell design balances turn position within each phase block. it does not fully counterbalance the chronological order in which the twelve API episodes run. a later sixteen-cell matrix can use a complete 4x4 Latin schedule if this pilot shows a signal worth estimating.

## cost gate

session 4 cost `$0.06411494`. twelve episodes at that observed cost project to `$0.76937928`.

the projection is not authorization. the frozen worst-case envelope is `$0.6173024` per cell and `$7.4076288` for twelve cells. the aggregate gate is therefore `$7.41`, with the existing `$0.62` per-cell runtime gate. no batch runs until both limits are explicitly approved.

## analysis

report the primary success count out of three for each phase, then the largest minus the smallest phase rate. report the secondary objective engine counts in the JSON manifest. do not call `n = 3` strong statistical evidence; it can expose a gross order effect and tell us whether a larger matrix is worth buying.

run any death postmortems only after all world cells finish. their linked artifacts and separate budget are outside the causal batch.
