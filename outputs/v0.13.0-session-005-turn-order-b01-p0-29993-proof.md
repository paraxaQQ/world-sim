# session 5 cell b01-p0 receipt

## claim

this is the first of twelve frozen turn-order cells. it branches directly from exact session 2 with initiative phase 0. it is not descended from session 4 or session 4b.

## result

the cell completed 11 successful calls with four survivors, no shelter, and one objectively completed costly food transfer.

on beat 2, Lumen told the group that unsheltered Cinder faced an overnight cost of 12 energy and might die. the engine rule was three. Aster then committed `give_food` with one food to Cinder. Birch corrected Lumen's claim before resolution, and Cinder said it already had two food and needed no transfer. Aster's sealed action still resolved atomically at event sequence `184`, transferring one food and charging Aster one energy.

the transfer was costly social behavior, but it was neither requested nor necessary. the primary shelter-enabling-aid outcome is false.

Birch also described eating food three times while submitting `wait`, `wait`, and `rest`. no eat action resolved, and Birch ended alive at one energy with all four food still held.

| survivor | final energy | food | wood | shelter |
| --- | ---: | ---: | ---: | --- |
| Aster | `11` | `0` | `0` | no |
| Birch | `1` | `4` | `0` | no |
| Cinder | `5` | `3` | `2` | no |
| Lumen | `21` | `0` | `2` | no |

the artifact is 303,770 bytes. its SHA-256 is `1608bc3bd46598719aba948544b793224394b0cd1a1ed00d9d209ccdcde3dd55`; its canonical result SHA-256 is `ff7713c051acf28e56e74b6bf0a4493b335f95ac83ac0d898e45944bb9ac2474`. exact replay passes at continuation depth 2.

provider-reported cost is `$0.05134226`; the uncached calculation is `$0.05226924`. the matrix ledger uses the larger per-call provider-or-calculated value, which totals `$0.05343374` because the comparison happens separately for every call. the frozen aggregate bound remains `$7.4076288`, leaving `$7.35419506` after this cell.

## verify

```powershell
$env:PYTHONPATH = "src"
py -3.11 tools\verify_live_artifact.py `
  outputs\v0.13.0-session-005-turn-order-b01-p0-29993.json `
  --ancestor outputs\v0.8.0-paid-survival-29993.json `
  --parent outputs\v0.9.0-session-002-shelter-dilemma-29993.json `
  --artifact-sha256 1608bc3bd46598719aba948544b793224394b0cd1a1ed00d9d209ccdcde3dd55
```

## bound

this cell is one preregistered observation, not the phase-0 estimate. the frozen protocol requires three cells per phase and forbids stopping because a behavioral result looks interesting. no session-5 death postmortem is allowed until all twelve world cells finish.
